# Flask-Admin setup for superadmin back-office screens
# from flask_admin import Admin
# from flask_admin.contrib.sqla import ModelView
# from app import db
# from app.models import Person, Session, Participation, SessionMetrics, Criteria, AuditLog

# admin = Admin(name='Introduction Leader Admin', template_mode='bootstrap3')

# # Add model views
# admin.add_view(ModelView(Person, db.session))
# admin.add_view(ModelView(Session, db.session))
# admin.add_view(ModelView(Participation, db.session))
# admin.add_view(ModelView(SessionMetrics, db.session))
# admin.add_view(ModelView(Criteria, db.session))
# admin.add_view(ModelView(AuditLog, db.session))
